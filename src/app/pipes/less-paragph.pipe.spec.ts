import { LessParagphPipe } from './less-paragph.pipe';

describe('LessParagphPipe', () => {
  it('create an instance', () => {
    const pipe = new LessParagphPipe();
    expect(pipe).toBeTruthy();
  });
});
