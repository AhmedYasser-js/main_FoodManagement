import { ParagraphLessPipe } from './paragraph-less.pipe';

describe('ParagraphLessPipe', () => {
  it('create an instance', () => {
    const pipe = new ParagraphLessPipe();
    expect(pipe).toBeTruthy();
  });
});
